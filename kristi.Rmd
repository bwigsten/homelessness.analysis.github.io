---
title: "Shiny Dashboard"
output: 
  flexdashboard::flex_dashboard:
    orientation: columns
    vertical_layout: fill
runtime: shiny
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
# calculating total percentage per state by year
count_total_state = 
  total_data %>% 
  ## removing those 2 random blank rows that appeared at the end of every dataset after the total
  subset(coc_name != "NA") %>% 
  group_by (year, state) %>% 
  mutate(
    th_state = sum(total_homeless) ## need to do this for each varaible of interest, then get breakdown of other variables based on state's overall percentage
  )
```








